spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kecdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#          javax:
#            persistence:
#             schema-generation:
#                create-source: metadata
#                scripts:
#                  action: create
#                  create-target: kec_db_create.sql
